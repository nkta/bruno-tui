meta {
  name: secret
  type: http
  seq: 1
}

get {
  url: http://127.0.0.1:9/never-called
  body: none
  auth: none
}

script:pre-request {
  const secret = bru.getEnvVar("oktaClientSecret");
  test("oktaClientSecret fourni", function() {
    expect(secret).to.equal("fixture-value");
  });
  // Aucune requête réseau : seul le test pré-requête compte.
  bru.runner.skipRequest();
}
